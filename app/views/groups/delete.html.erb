<h1>
	<%= link_to("\u2B05", {:action => 'index'}, :class => 'back-link symb') %>
	Are you sure you want to delete <u><%= @group.name %></u>?
</h1>
<%= form_for(:group, :url => {:action => 'destroy', :id => @group.id}) do |f| %>
	<%= submit_tag("YES") %>
<% end %>
<ul>
	<li><%= @group.people.size %></li>
	<li><%= @group.visible ? 'Visible' : 'Invisible' %></li>
	<li>Created at <%= @group.created_at %></li>
	<li>Last updated at <%= @group.updated_at %></li>
</ul>
