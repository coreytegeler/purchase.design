<h1>
	<%= link_to("\u2B05", {:action => 'admin'}, :class => 'back-link symb') %>
	<%= @alumnus.name %>
</h1>

<%= form_for(:alumnus, :url => {:action => 'update', :id => @alumnus.id}) do |f| %>
	<%= render(:partial => 'form', :locals => {:f => f}) %>
	<%= submit_tag("Update") %>
<% end %>