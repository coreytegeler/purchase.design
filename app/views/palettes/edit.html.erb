<h1>
	<%= link_to("\u2B05", {:controller => 'palettes', :action => 'admin'}, :class => 'back-link symb') %>
	<%= @palette.name %>
</h1>
<%= form_for(:palette, :url => {:action => 'update', :id => @palette.id}) do |f| %>
	<%= render(:partial => "form", :locals => {:f => f}) %>
	<%= submit_tag("Update") %>
<% end %>
