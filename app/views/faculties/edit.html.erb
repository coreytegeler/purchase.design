<h1>
	<%= link_to("\u2B05", {:controller => 'faculties', :action => 'admin'}, :class => 'back-link symb') %>
	<%= @faculty.name %>
</h1>

<%= form_for(:faculty, :url => {:action => 'update', :id => @faculty.id}) do |f| %>
	<%= render(:partial => 'form', :locals => {:f => f}) %>
	<%= submit_tag("Update") %>
<% end %>