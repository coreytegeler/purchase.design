<div id="delete">
	<div class="vert_center">
		<div class="popup">
			<div class="inner">
				<div class="vert_center">
					<h1>
						Did <%= faculty.first_name %> stop working here?
					</h1>

					<div class="decision">
						<%= form_for(:faculty, :url => {:action => 'destroy', :id => faculty.id}) do |f| %>
							<%= submit_tag("YES") %>
						<% end %>
						<%= submit_tag("NO", :id => 'close', :onClick => "$('#delete').remove()") %>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>