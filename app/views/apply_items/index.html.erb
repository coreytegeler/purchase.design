<header>

</header>
<section class="masonry" id="apply">
	<% @apply_items.each do |item| %>
		<div class="apply_item block">
			<div class="box secondary_fill" <%= color('border', @s) %>>
				<%= inline_svg('check', false) %>
			</div>
			<div class="text medium">
				<%= item.text.html_safe %>
			</div>
		</div>
	<% end %>
</section>
<%= link_to 'Apply!', 'http://www.purchase.edu/departments/admissions/', {:id => 'apply_btn', :class => 'text large', :target => '_blank'} %>