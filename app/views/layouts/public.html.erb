<!DOCTYPE html>
<html lang="en" xml:lang="en" xmlns= "http://www.w3.org/1999/xhtml">
<head>
	<title>Purchase College Graphic Design BFA Program</title>
	<%= stylesheet_link_tag "application", params[:controller], :media => "all", 'data-turbolinks-track' => true %>
	<%= stylesheet_link_tag "animate", :media => "all", 'data-turbolinks-track' => true %>
	<%= stylesheet_link_tag "public", :media => "all", 'data-turbolinks-track' => true if !public? %>
	<%= javascript_include_tag "application", params[:controller], 'data-turbolinks-track' => true %>
	<%= javascript_include_tag "public", 'data-turbolinks-track' => true if !public? %>
	<%= csrf_meta_tags %>
	<meta charset="UTF-16">
	<meta name="description" content="Graphic Design BFA Program, School of Art + Design, Purchase College, SUNY">
	<meta name="keywords" content="Purchase College, SUNY, State University of New York, Visual Arts, Art+Design, art, design, graphic design, graphic design department, BFA, graphic design BFA, bachelors of fine arts, university, college, education">
	<meta name="google" content="notranslate">
	<meta http-equiv="Content-Language" content="en">
</head>
<body class="public <%= @gradient_state %> <%= @pattern_state %>" id="<%= params[:controller].parameterize %>_controller" <%= color('bg', @p, 'text', @s, 'border', @s) %>>
	<div id="gradient">
		<%= render 'public/gradient_bg', :gradient => @gradient %>
	</div>
	<div id="pattern">
		<%= render 'public/pattern_bg', :pattern => @pattern %>
	</div>
	<% if !flash[:notice].blank? %>
		<div class="notice <%= flash[:type] %>">
			<div class="bar">
				<div class="vert">
					<%= flash[:notice].html_safe %>
				</div>
			</div>
		</div>
	<% end %>
	<main>
		<%= render 'public/top_bar' %>

		<aside class="static" id="side_bar">
			<%= render 'public/side_bar' %>
		</aside>
		
		<div id="border" <%= color('border', @s) %>>
			<section id="page_content">
				<%= yield %>
			</section>
		</div>

		<%= render 'public/about' %>
		
	</main>
	<div id="hiddenPalette" data-primary="<%= @p %>" data-secondary="<%= @s %>"></div>
</body>
</html>
