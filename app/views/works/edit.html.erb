<h1>
	<%= link_to("\u2B05", {:action => 'index'}, :class => 'back-link symb') %>
	<%= @work.title %>
</h1>

<%= form_for(:work, :url => {:action => 'update', :id => @work.id}) do |f| %>
	<%= render(:partial => 'form', :locals => {:f => f}) %>
	<%= submit_tag("Update") %>
<% end %>