<h1>
	<%= link_to("\u2B05", {:action => 'index'}, :class => 'back-link symb') %>
	Are you sure you want to delete <u><%= @person.name %></u>?
</h1>
<%= form_for(:person, :url => {:action => 'destroy', :id => @person.id}) do |f| %>
	<%= submit_tag("YES") %>
<% end %>
<ul>
	<li><%= @person.work.size %></li>
	<li><%= @person.visible ? 'Visible' : 'Invisible' %></li>
	<li>Created at <%= @person.created_at %></li>
	<li>Last updated at <%= @person.updated_at %></li>
</ul>
