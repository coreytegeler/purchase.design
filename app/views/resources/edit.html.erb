<h1>
	<%= link_to("\u2B05", {:action => 'admin'}, :class => 'back-link symb') %>
	<%= @resource.name %>
</h1>

<%= form_for(:resource, :url => {:action => 'update', :id => @resource.id}) do |f| %>
	<%= render(:partial => 'form', :locals => {:f => f}) %>
	<%= submit_tag("Update") %>
<% end %>