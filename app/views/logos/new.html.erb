<h1>
	<%= link_to("\u2B05", {:controller => 'palettes', :action => 'admin'}, :class => 'back-link symb') %>
	New palette
</h1>
<%= form_for(:palette, :url => {:action => 'create', :id => @palette.id}) do |f| %>
	<%= render(:partial => "form", :locals => {:f => f}) %>
	<%= submit_tag("Create") %>
<% end %>